# This rule is not used by the project fm-crd itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the fleetmanagement.grafana.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: fleet-management-operator
    app.kubernetes.io/managed-by: kustomize
  name: pipeline-editor-role
rules:
- apiGroups:
  - fleetmanagement.grafana.com
  resources:
  - pipelines
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - fleetmanagement.grafana.com
  resources:
  - pipelines/status
  verbs:
  - get
