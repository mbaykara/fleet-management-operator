{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "fleet-management-operator.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "fleet-management-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "fleet-management-operator.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "fleet-management-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
